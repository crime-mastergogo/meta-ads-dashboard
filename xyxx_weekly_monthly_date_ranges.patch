*** Begin Patch
*** Update File: generate_weekly.py
@@
 def short_name(n):
@@
     return n[:52]

+def week_date_range(week_str):
+    year = int(week_str[:4])
+    week = int(week_str[-2:])
+    jan1 = date(year, 1, 1)
+    first_monday = jan1 + __import__("datetime").timedelta(days=(7 - jan1.weekday()) % 7)
+    start = first_monday + __import__("datetime").timedelta(weeks=week - 1)
+    return start, start + __import__("datetime").timedelta(days=6)
+
 def ad_card(ad, cfg, is_best=False, is_worst=False):
@@
     week_str = now.strftime("%Y-W%W")
     date_str = now.strftime("%d %b %Y")
+    range_start, range_end = week_date_range(week_str)
+    range_str = f"{range_start.strftime('%d %b %Y')} – {range_end.strftime('%d %b %Y')}"
@@
-  <div class="cover-sub">Last {lookback} days rolling · 1D Click Attribution · Updated {date_str}</div>
+  <div class="cover-sub">{range_str} · Last {lookback} days rolling · 1D Click Attribution · Updated {date_str}</div>
@@
-<div class="footer">XYXX Crew · {week_str} · Auto-generated by GitHub Actions · 1D Click</div>
+<div class="footer">XYXX Crew · {week_str} · {range_str} · Auto-generated by GitHub Actions · 1D Click</div>
@@
 def build_weekly_index(cfg):
@@
-    links = "".join(
-        f'<li><a href="{pages_base}/weekly/{r}">{r.replace(".html","")}</a></li>'
-        for r in reports
-    )
+    links = ""
+    for r in reports:
+        week_str = r.replace(".html", "")
+        start, end = week_date_range(week_str)
+        range_str = f"{start.strftime('%d %b %Y')} – {end.strftime('%d %b %Y')}"
+        links += f'<li><a href="{pages_base}/weekly/{r}">{week_str}</a><span>{range_str}</span></li>'
@@
-<style>body{{background:#0c0c0c;color:#e8e8e8;font-family:Inter,sans-serif;padding:40px;}}
-h1{{font-size:20px;margin-bottom:20px;}}ul{{list-style:none;}}li{{margin-bottom:10px;}}
-a{{color:#60a5fa;text-decoration:none;font-size:14px;}}a:hover{{text-decoration:underline;}}</style>
+<style>body{{background:#0c0c0c;color:#e8e8e8;font-family:Inter,sans-serif;padding:40px;}}
+h1{{font-size:20px;margin-bottom:20px;}}ul{{list-style:none;padding:0;}}li{{margin-bottom:14px;}}
+a{{color:#60a5fa;text-decoration:none;font-size:14px;display:block;}}a:hover{{text-decoration:underline;}}
+span{{display:block;color:#666;font-size:11px;margin-top:3px;}}</style>
*** Update File: generate_monthly.py
@@
 def short_name(n):
@@
     return n[:52]

+def month_date_range(month_str):
+    year, month = map(int, month_str.split("-"))
+    start = date(year, month, 1)
+    if month == 12:
+        end = date(year + 1, 1, 1) - timedelta(days=1)
+    else:
+        end = date(year, month + 1, 1) - timedelta(days=1)
+    return start, end
+
 def ad_card(ad, cfg, is_best=False, is_worst_roas=False, is_bottom=False):
@@
     month_str  = last_prev.strftime("%Y-%m")
     month_label = last_prev.strftime("%B %Y")
+    range_start, range_end = month_date_range(month_str)
+    range_str = f"{range_start.strftime('%d %b %Y')} – {range_end.strftime('%d %b %Y')}"
@@
-    {since} → {until} · 1D Click Attribution · Consolidated across campaigns ·
+    {range_str} · 1D Click Attribution · Consolidated across campaigns ·
@@
-  XYXX Crew · {month_label} · Consolidated by creative · Min ₹10K spend for lowest performers ·
+  XYXX Crew · {month_label} · {range_str} · Consolidated by creative · Min ₹10K spend for lowest performers ·
@@
 def build_monthly_index(cfg):
@@
-    links = "".join(
-        f'<li><a href="{pages_base}/monthly/{r}">{r.replace(".html","")}</a></li>'
-        for r in reports
-    )
+    links = ""
+    for r in reports:
+        month_str = r.replace(".html", "")
+        try:
+            start, end = month_date_range(month_str)
+            month_label = start.strftime("%B %Y")
+            range_str = f"{start.strftime('%d %b %Y')} – {end.strftime('%d %b %Y')}"
+        except ValueError:
+            month_label, range_str = month_str, ""
+        links += f'<li><a href="{pages_base}/monthly/{r}">{month_label}</a><span>{range_str}</span></li>'
@@
-<style>body{{background:#0c0c0c;color:#e8e8e8;font-family:Inter,sans-serif;padding:40px;}}
-h1{{font-size:20px;margin-bottom:20px;}}ul{{list-style:none;}}li{{margin-bottom:10px;}}
-a{{color:#60a5fa;text-decoration:none;font-size:14px;}}a:hover{{text-decoration:underline;}}</style>
+<style>body{{background:#0c0c0c;color:#e8e8e8;font-family:Inter,sans-serif;padding:40px;}}
+h1{{font-size:20px;margin-bottom:20px;}}ul{{list-style:none;padding:0;}}li{{margin-bottom:14px;}}
+a{{color:#60a5fa;text-decoration:none;font-size:14px;display:block;}}a:hover{{text-decoration:underline;}}
+span{{display:block;color:#666;font-size:11px;margin-top:3px;}}</style>
*** End Patch
